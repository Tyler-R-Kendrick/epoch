{
  "name": "@epoch/live",
  "version": "0.1.0",
  "description": "Browser client for signed live state: a rollback and data-propagation store competing with Redux and Yjs.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/integration-core": "0.1.0",
    "@epoch/wasm-react": "0.1.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    }
  }
}
