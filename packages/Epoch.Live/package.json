{
  "name": "@epoch/live",
  "version": "0.1.0",
  "description": "Framework-agnostic browser client for signed live state with durable rollback and peer data propagation.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/integration-core": "0.1.0",
    "@epoch/wasm-react": "0.1.0"
  }
}
