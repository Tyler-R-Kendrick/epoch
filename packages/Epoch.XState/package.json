{
  "name": "@epoch/xstate",
  "version": "0.1.0",
  "description": "Explicit XState tracking observers for Epoch.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/integration-core": "0.1.0"
  },
  "peerDependencies": {
    "xstate": ">=5.0.0"
  }
}
