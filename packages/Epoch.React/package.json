{
  "name": "@epoch/react",
  "version": "0.1.0",
  "description": "React provider and hooks for Epoch integration packages.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/integration-core": "0.1.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  }
}
