{
  "name": "@epoch/redux",
  "version": "0.1.0",
  "description": "Explicit Redux tracking middleware for Epoch.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/integration-core": "0.1.0"
  },
  "peerDependencies": {
    "redux": ">=5.0.0"
  }
}
