{
  "name": "@epoch/software-heritage",
  "version": "0.1.0",
  "description": "SWHID v1.2 identifiers, Git-compatible hashing, and fail-closed archival client.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": { "build": "tsgo -p tsconfig.json", "typecheck": "tsgo -p tsconfig.json --noEmit", "test": "npm run build && node --test test/*.test.mjs" }
}
