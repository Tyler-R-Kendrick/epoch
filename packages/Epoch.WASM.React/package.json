{
  "name": "@epoch/wasm-react",
  "version": "0.1.0",
  "description": "React hooks for browser-safe Epoch WASM state history.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  }
}
