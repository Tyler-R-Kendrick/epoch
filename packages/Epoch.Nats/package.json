{
  "name": "@epoch/nats",
  "version": "0.1.0",
  "description": "NATS realtime fabric for Epoch: JetStream streams, auth callout, and live subjects (ADR-0054).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.mjs"
  }
}
