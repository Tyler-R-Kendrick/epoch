{
  "name": "@epoch/identity-bridge",
  "version": "0.1.0",
  "description": "Nostr ↔ AT Protocol identity bridge (mutual dual-signed bindings, pure verifier, witness index).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/core": "0.1.0",
    "@epoch/atproto": "0.1.0",
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0",
    "zod": "^4.4.3"
  }
}
