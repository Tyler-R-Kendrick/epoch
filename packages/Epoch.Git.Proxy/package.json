{
  "name": "@epoch/git-proxy",
  "version": "0.1.0",
  "description": "Git smart-HTTP façade and live migration workers over Epoch repositories.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/core": "0.1.0"
  }
}
