{
  "name": "@epoch/semantic",
  "version": "0.1.0",
  "description": "Browser-safe semantic content pipeline for Epoch: syntax providers, structural diff and patch, structural three-way merge, and semantic compression planning.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  }
}
