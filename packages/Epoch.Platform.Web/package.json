{
  "name": "@epoch/platform-web",
  "version": "0.1.0",
  "description": "Epoch hosting control-plane PWA for deploying Epoch-related services.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/design-tokens": "0.1.0"
  }
}
