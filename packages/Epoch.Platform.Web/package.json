{
  "name": "@epoch/platform-web",
  "version": "0.1.0",
  "description": "Browser-rendered Epoch.Platform web console foundation.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  }
}
