{
  "name": "@epoch/community-core",
  "version": "0.1.0",
  "description": "Epoch Community API client and shared community domain types.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "test": "npm run build && node test/convergence.test.mjs && node test/projection-compiler.test.mjs && node test/namespace-runtime.test.mjs",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/protocol": "0.1.0"
  }
}
