{
  "name": "@epoch/xmpp",
  "version": "0.1.0",
  "description": "Epoch-native XMPP s2s FederationTransport adapter (loss-declared; JIDs are admission only).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.mjs"
  }
}
