{
  "name": "@epoch/community-web",
  "version": "0.1.0",
  "description": "Separate Epoch Community PWA for repository collaboration workflows.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@epoch/community-core": "0.1.0"
  }
}
