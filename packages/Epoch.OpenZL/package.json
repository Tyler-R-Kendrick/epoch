{
  "name": "@epoch/openzl",
  "version": "0.1.0",
  "description": "Host OpenZL entropy codec for Epoch blobs, artifacts, and semantic changediffs (ADR-0053).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.mjs"
  }
}
