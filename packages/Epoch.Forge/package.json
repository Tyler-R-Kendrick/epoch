{
  "name": "@epoch/forge",
  "version": "0.1.0",
  "description": "Forge-neutral mirror authority and reconciliation contracts for Epoch.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  }
}
