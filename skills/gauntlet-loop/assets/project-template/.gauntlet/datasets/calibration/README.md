# Calibration split

Known-good, known-bad, tie, and abstention cases used by `gauntlet evaluate
calibrate` to validate deterministic and learned evaluators. Calibration
cases never decide promotion and are never shown to candidates as search
targets.
