# Runtime state (ignored)

ActiveGraph SQLite store and sidecars. Durable local runtime truth; the
portable evidence lives in `../ledger/` and exported bundles. Never edit
or query these files directly — use `gauntlet` commands.
