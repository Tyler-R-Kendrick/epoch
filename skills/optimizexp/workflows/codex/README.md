# Codex — OptimizeXP review workflow

Codex has no Rhai workflow runtime in-repo. Use [review.md](review.md) as the step script the agent follows, and optionally `../cross-agent/review-loop.mts` for deterministic scoring helpers.
