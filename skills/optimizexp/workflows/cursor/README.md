# Cursor — OptimizeXP review workflow

Cursor uses `.agents/skills/` + rules. Drive the loop from [review.md](review.md). Always-on caveman rules may compress output; keep bus JSON complete (do not caveman-truncate JSON fields).
