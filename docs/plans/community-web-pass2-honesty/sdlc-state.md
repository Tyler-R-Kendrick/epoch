# Community Web pass-2 honesty SDLC state

- Phase: implemented
- Initiative: board honesty of verbs, sample/live, receipts, AT OAuth, live Activity
- Source: competitor pass-2 aggregate (2026-08-13) on `epoch-vcs.vercel.app`

## Layers

1. Sample stream honesty — implemented
2. Working / unsigned / composer honesty — implemented
3. Receipts as objects; Jump does not wipe search — implemented
4. Promote as a real verb — implemented
5. Keyboard contract + Following — implemented
6. Mute / report / hook scope — implemented
7. Real AT OAuth — implemented (PAR/PKCE/DPoP, injectable host)
8. Live multi-user Activity — implemented (store events only; sample board never ticks)
9. Targeting leftovers — implemented
10. Secure command livestream — implemented

## Gap pass

Receipt blade paints actor/evidence; Jump no longer types `zi`; the jump chooser stays open as a layer after Enter clears the prompt; fixture agents stay idle without a heartbeat; mute/report sit on the post tray and muted objects drop hook Activity; `ScopedOAuthClient` refuses minted `epoch-oauth-*` tokens; `tick()` never invents `live-*` posts.

## Layer 01 notes

Fixture board: `SAMPLE STREAM` banner, no `tick()` interval, no live member counts, landing enter opens `#general`.
